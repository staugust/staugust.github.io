# Netlink 

## commands in `iproute2`

+ ip
+ tc
+ ss
+ lnstat
+ bridge

## commands in `net-tools`
`net-tools` is based on IOCTL(Input/Output Control). 
+ ifconfig
+ arp
+ route
+ netstat
+ hostname
+ rarp
